你正在维护一个名为 `word-test` 的全栈项目。请直接在现有代码基础上完成以下功能新增与问题修复，输出可运行结果，不要只给建议。

目标：
优化词库页的使用流程，新增默认账户、答题反馈音效、会员系统占位、双测试模式，并修复若干交互问题。

====================
一、Add Feature
====================

1. 增加系统默认账户
请在系统初始化阶段内置一个默认账户，方便演示和测试。

要求：
- user id: admin
- email: admin
- password: admin

实现要求：
- 如果数据库中不存在该账户，则自动创建
- 如果已存在，则不要重复创建
- 默认账户应能正常登录
- 如果 Better Auth / 用户表对 email 格式有要求，请用兼容当前系统的最小改法实现，并说明处理方式
- 优先通过 seed 或初始化逻辑实现，不要手动改数据库

2. 答对单词后播放提示音，并支持关闭
在测试页面中，当用户拼写正确时，自动播放一个内置提示音，增强反馈感。

要求：
- 答对后自动播放提示音
- 提供一个明显的开关 / 按钮，让用户可以关闭该功能
- 关闭后，本轮测试和后续测试都应生效，尽量持久化用户偏好（例如 localStorage）
- 音频资源请放在项目内，使用内置静态资源，不依赖外部链接
- 不要影响原有“播放单词音频”的功能

3. 增加会员系统占位页 / 板块
先做一个会员系统的前端板子，后续再考虑接支付接口。

要求：
- 新增一个“会员”相关页面或仪表盘板块
- 展示基础会员方案信息，例如：
  - 免费用户
  - 普拉斯用户
- 展示占位内容，例如：
  - 会员权益对比{
    - 免费用户: 5个自定义词库 单个词库最高可存100单词
    - 普拉斯用户: 不限词库数量 单个词库最高可存500单词
  }
  - 后续支持支付接口接入
  - 解锁更多功能（先作为文案占位）
  - 普拉斯用户价格：114514元终身
- 当前阶段不需要真正支付，不需要接第三方支付 API
- UI 要与现有项目风格统一，保持简洁现代
- 普拉斯用户的相关内容用金色主题设计，凸显至尊感。

4. 新增测试模式选择
当用户点击某个词库进入测试时，不要直接进入单一测试流程，而是先让用户选择测试模式。

新增两种测试方式：
- 汉语翻译成英语测试，展示汉语，测试英语
- 语音读音翻译成英语单词测试（保留当前模式）

要求：
- 用户点击“开始测试”后，先看到测试模式选择
- 选择后进入对应测试流程
- 当前已有的“听音拼写”逻辑继续保留
- 新增“中文 -> 英文”模式：
  - 页面展示中文含义
  - 用户输入英文单词
  - 系统判断正误
  - 记录测试结果
- 两种模式都要接入已有学习记录体系
- 如有必要，可为 test_records 增加字段标记测试模式，但不要过度重构

====================
二、Bug Fix
====================

1. 词库页布局顺序优化（你说的“上下页面反转”）
这里的意思不是整个页面上下翻转，而是：
当前页面中，“新建词库”区域在上方，“已有词库/使用词库测试”区域在下方，不利于用户直接开始学习。

请调整页面布局，使“使用词库测试”相关区域优先展示，提升使用效率。

明确要求：
- 将“已有词库 / 默认词库 / 我的词库 / 开始测试”区域移动到更靠前、更显眼的位置
- 将“新建词库”区域放到后面
- 目标是：用户进入页面后，先看到可直接使用和开始测试的内容，再看到创建词库的表单
- 保持整体布局整洁，不要破坏响应式
- 可以调整为上下顺序交换，或在同屏内重排，但核心目标是“测试入口优先，新建词库靠后”

2. 错题页面无法重新播放单词发音
修复错题页中无法重新聆听单词发音的问题。

要求：
- 每个错词都应提供播放音频的按钮
- 点击后可重复播放
- 如果该单词没有音频，要显示明确提示
- 不要只修 UI，要真正保证功能可用

====================
三、实现要求
====================

1. 保持当前技术栈不变
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth
- Zod

2. 代码要求
- 直接修改现有项目
- 保持代码可运行
- 优先可读性和可维护性
- 不要过度抽象
- 尽量复用现有组件与逻辑
- 必要时可新增少量文件
- 为关键逻辑补充简洁注释

3. 数据与兼容性
- 默认账户的初始化不能破坏现有用户数据
- 新增测试模式不能破坏现有测试记录
- 如果需要新增数据库字段，请同步更新 schema、迁移/推送、seed
- 如果会员系统当前只是占位，请明确哪些是占位实现

====================
四、验收标准
====================

完成后请确保以下内容都可验证：

1. 默认账户可登录
- admin / admin（以及 email 为 admin，若系统要求邮箱格式则说明兼容处理）

2. 答对单词后会自动播放提示音
- 且用户可以关闭该功能
- 刷新页面后关闭状态尽量保留

3. 项目中出现会员系统入口或页面
- 当前为前端占位，但结构清晰

4. 点击词库测试时，可先选择测试模式
- 中文 -> 英文
- 语音 -> 英文

5. 词库页布局已优化
- 用户优先看到“可直接开始测试”的区域
- 新建词库区域放到后面

6. 错题页可重新播放音频
- 有音频时能播
- 无音频时有降级提示

====================
五、输出要求
====================

完成后请输出：
1. 修改了哪些文件
2. 每个文件的核心改动
3. 新增了哪些功能
4. 修复了哪些 bug
5. 如何验证这些改动
6. 哪些部分目前只是“占位实现”