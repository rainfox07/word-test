<!-- add feature:

- 在单词领读界面：
    -不要一开始进入界面就播放，先弹出页面让用户设置。然后在单词播放的时候有一个按钮可以再设置播放的页面，也是弹出的界面。
    - 单词和汉语意思的字号变得更大，并且在屏幕剧中，目的是适应多媒体的环境。
    - 添加一个播放暂停的按钮。
    - 添加一个返回主页的按钮。
    - 记录当前的学习状态，只展示：已完成学习/未完成学习； 分别为绿色的样式和红色的样式

- 在主页增加模块：
    - 不知道怎么使用？查看视频教程。然后指向一个视频url，视频我还没录好，现在先给个提示说：视频未录制
    - 您是老师或教育工作者？有意向与我合作或提出意见？点此联系我。点击联系我按钮后弹出界面：
        - 我能为您做什么？
        1. 自定义单词功能，无论是学习、测试、单词游戏等自定义功能
        2. 自定义您的单词词库，可以是课本或您自己的词库
        3. 不受限制的词库与单词数量设置
        4. 将您的团队/单位的logo标识放在主页或您指定的位置，更加醒目，彰显品牌文化。
        5. 将网站完全自定义成您想要的样子，或者打包成app，随您所欲。
        - 需要您做什么？
        1. 添加我的联系方式。
        2. 说出您的需求。
        3. 由我们协商价格后，您需要先支付30%的费用。
        4. 等待项目的完成。通常为几周或几月。
        5. 交付项目，并提供售后服务。
        6. 您支付余下70%的费用。

请调整词库管理页面当前的两栏结构，把它从“默认词库 + 我的词库”改成“三栏结构”。

具体要求如下：

1. 页面结构调整
- 当前只有两个栏目：
  - 默认词库
  - 我的词库
- 请改成三个栏目，顺序必须是：
  1. 默认词库
  2. 选择课本
  3. 我的词库

2. 新增中间栏目：选择课本
- 在默认词库和我的词库之间新增一个独立栏目，标题为“选择课本”
- 这个栏目未来用于承载课本数据
- 目前系统内还没有导入课本数据，所以当前可以显示为空状态
- 空状态文案要合理，例如：
  - “暂无课本数据”
  - “后续可导入课本内容”
- 但这个模块的结构要先搭好，不要只是写一行字敷衍处理

3. 课本数据后续可扩展
- 虽然现在还没有课本数据，但请把这个栏目设计成后续可以随时导入数据并展示
- 也就是说，这个不是纯静态占位，而是要预留和当前词库类似的列表展示结构
- 后续如果导入课本内容，应能像其他栏目一样显示卡片列表

4. 三个栏目标题前增加 emoji 区分
请在这三个栏目标题前分别加一个 emoji，方便用户快速区分模块。

例如可以采用：
- 默认词库：📚 默认词库
- 选择课本：📖 选择课本
- 我的词库：🗂️ 我的词库

如果你有更合适的 emoji，也可以替换，但要符合语义、风格统一。

5. 布局要求
- 三个栏目保持统一风格
- 保持现有页面简洁、现代、学习产品风格
- 响应式布局不能坏
- 在宽屏下优先展示为三栏
- 在窄屏下可以自然折叠为上下排列
- 三栏的卡片宽度、高度、边距、圆角、阴影要协调一致

6. 数据与实现要求
- 默认词库和我的词库保持现有功能不变
- 新增“选择课本”栏目时，不要影响现有词库逻辑
- 课本栏目现在没有数据时，显示空状态
- 但代码结构上要便于后续接入课本数据源

验收标准：
- 页面由两栏变为三栏
- 顺序为：默认词库、选择课本、我的词库
- 三个栏目标题前都有 emoji
- 中间“选择课本”栏目现在可显示空状态
- 后续可扩展为课本数据展示区
- 现有默认词库和我的词库功能保持正常

你正在维护一个名为 `word-test` 的全栈项目。请直接在现有代码基础上，对“仪表盘”页面进行一次完整但克制的重构优化。不要只给建议，要输出可运行结果。

目标：
把当前仪表盘从“静态统计页”优化为“学习控制台”，让用户进入页面后能够：
1. 快速看懂当前学习状态
2. 明确下一步该做什么
3. 获得轻量的学习反馈与进度感
4. 保持当前项目简洁、现代、学习类产品风格

====================
一、整体重构方向
====================

请将仪表盘重构为更偏“学习控制台”的页面，而不是单纯的数据展示页。

设计原则：
- 优先引导用户开始下一步学习
- 数据展示服务于学习行为，而不是只做统计堆叠
- 保持信息层级清晰
- 保持风格简洁、现代、轻量
- 响应式布局不能坏
- 不要过度炫技，不要做复杂可视化图表

====================
二、页面结构建议（请按此思路实现）
====================

请将仪表盘重构为以下几个主要区域：

1. 顶部欢迎区 + 主操作区
当前“欢迎回来, rainfox”可以保留，但要优化成更有引导性的结构。

要求：
- 保留欢迎语
- 增加一句更有产品感的副文案，例如：
  - “今天继续完成你的单词练习”
  - “从这里继续测试、复习或领读”
- 在欢迎区旁边或下方加入一个明显的主操作区

主操作区建议包含：
- 开始今日测试
- 去错词复习
- 进入词库管理
- 单词领读（如果项目里已经有领读功能，则接入；如果没有则先做按钮占位或跳转到词库）

要求：
- 至少有一个最突出的主按钮（primary CTA）
- 不要再只是“进入词库并开始测试”这种模糊卡片，要改成更明确的动作入口
- 视觉上这个区域要成为仪表盘第一关注点

2. 学习概览卡片区
请保留顶部概览卡片，但优化为更有学习意义的内容。

建议优先展示以下 4 项（如已有数据可直接接入，没有的数据可根据现有数据合理占位或推导）：
- 可学习词库数量
- 待复习错词数量
- 最近学习记录数量 / 今日学习量
- 最近正确率 / 近期正确率

实现要求：
- 卡片数量控制在 3~4 个，不要太多
- 卡片文案要更自然、更像学习产品
- 数字层级要明显
- 状态颜色使用克制，不要过多高饱和色
- “错词”可以继续保留红色倾向，但不要刺眼
- 如果某项数据暂时没有可靠来源，可以先做占位式展示，但必须说明

3. 推荐动作 / 下一步建议区
新增一个“推荐动作”或“下一步建议”模块，增强行为引导。

示例方向：
- 如果有错词：优先建议“去复习错词”
- 如果错词为 0：建议“开始新一轮测试”
- 如果某个词库完成度较高但未完成：建议“继续完成 XX 词库”
- 如果新建了词库还没测试：建议“开始第一次测试”

要求：
- 至少展示 1 条推荐动作
- 推荐逻辑可以先用简单规则实现
- 文案要明确，不要空泛
- 这个模块不需要复杂算法，MVP 可用即可

4. 最近学习记录区优化
保留“最近学习记录”模块，但要重构得更紧凑、更清晰。

要求：
- 默认只显示最近 5 条记录，不要把页面拉得太长
- 增加“查看全部”或跳转入口（如果已有相关页则复用）
- 每条记录要更紧凑，突出重点信息
- 每条建议展示：
  - 单词
  - 中文释义
  - 用户答案（如有必要）
  - 所属词库
  - 对错状态
  - 时间
- 优化状态标签视觉样式，例如：
  - 正确：绿色轻标签
  - 错误：红色轻标签
- 不要让每条记录显得过于冗长

5. 页面节奏与留白优化
当前页面信息分布偏平，请优化整体视觉节奏。

要求：
- 顶部欢迎区、概览区、推荐区、记录区之间的层级更清晰
- 不要让所有模块都长得一模一样
- 控制留白与卡片间距，让页面更有呼吸感
- footer 仍应保持 sticky footer 逻辑，不要再破坏全局布局

====================
三、文案优化要求
====================

请顺手优化仪表盘中的文案，使它更像学习产品，而不是后台面板。

例如可将类似文案优化为更自然的表达：
- “可学习词库” → “可开始学习的词库”
- “错词数量” → “待复习错词”
- “下一步” → “继续学习”
- “进入词库并开始测试” → “开始今日测试”

要求：
- 文案简洁
- 语气自然
- 避免生硬的管理后台语言
- 保持中文界面统一风格

====================
四、实现要求
====================

1. 直接修改现有项目代码
- 不要只分析
- 不要只给伪代码
- 输出应可运行

2. 保持当前技术栈不变
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth

3. 代码要求
- 优先保证可运行
- 优先保证 UI 一致性
- 不要过度抽象
- 尽量复用现有组件
- 必要时可新增少量组件
- 对关键重构点加简洁注释

4. 样式要求
- 保持简洁、现代、学习类产品风格
- 不要做花哨的大屏风格
- 不要引入复杂图表库
- 响应式布局保持正常
- 桌面端结构清晰，移动端自然折叠

====================
五、验收标准
====================

完成后请确保以下内容可验证：

1. 仪表盘顶部有欢迎区，并有更明确的学习引导
2. 页面中存在一个明显的主操作区，而不是只有普通统计卡片
3. 学习概览卡片更有学习意义，不只是静态资源数量
4. 页面中有“推荐动作 / 下一步建议”模块
5. 最近学习记录只显示最近 5 条，并支持查看更多
6. 仪表盘整体视觉层级比现在更清晰
7. 文案更像学习产品而不是后台系统
8. 不破坏现有数据逻辑与页面功能

====================
六、输出要求
====================

完成后请输出：
1. 修改了哪些文件
2. 每个文件的核心改动
3. 仪表盘结构重构了哪些部分
4. 哪些数据是真实接入的
5. 哪些模块目前是轻量规则 / MVP 实现
6. 如何验证这次重构

        
 -->


你正在维护一个名为 `word-test` 的全栈项目。请直接在现有代码基础上完成以下功能新增与页面优化，输出可运行结果，不要只给建议，也不要只写伪代码。

目标：
继续完善学习体验，重点优化以下四个部分：
1. 单词领读页面
2. 主页新增引导与合作联系模块
3. 词库管理页三栏重构
4. 仪表盘重构为“学习控制台”

整体要求：
- 保持当前项目简洁、现代、学习类产品风格
- 保持中文界面统一
- 不要过度炫技，不要做复杂图表
- 响应式布局不能坏
- 优先保证可运行、可验证、可维护

技术栈保持不变：
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth

==================================================
一、单词领读页面优化
==================================================

请优化“单词领读”功能页面，提升其在多媒体教学场景下的可用性。

1. 首次进入时不要立即播放
- 用户进入单词领读页面后，不要直接开始播放
- 应先弹出一个“播放设置”界面 / modal / drawer
- 用户确认设置后，才正式开始领读

2. 播放过程中支持重新打开设置
- 在单词播放界面中，新增一个“设置”按钮
- 点击后可再次弹出播放设置界面
- 用户可以重新调整领读参数

3. 单词与中文释义字号放大，并居中展示
- 当前单词与中文释义请改为更大的字号
- 在页面中尽量居中显示
- 目标是适应投屏、多媒体教学、远距离观看场景
- 页面视觉重点应放在“当前单词”和“中文释义”本身

4. 新增播放 / 暂停按钮
- 在领读界面中增加明显的播放 / 暂停按钮
- 能控制当前领读流程的暂停与继续
- 不要破坏现有自动播放逻辑

5. 新增返回主页按钮
- 在领读界面中增加一个“返回主页”按钮
- 点击后返回首页
- 风格与页面按钮体系保持一致

6. 增加学习状态展示
- 在领读界面中记录并展示当前学习状态
- 只需要展示两种状态：
  - 已完成学习
  - 未完成学习
- 样式要求：
  - 已完成学习：绿色
  - 未完成学习：红色
- 这是当前领读进度的轻量状态提示，不需要复杂统计图表

==================================================
二、主页新增两个模块
==================================================

请在主页中新增两个引导模块，提升用户理解成本与合作转化能力。

1. 视频教程入口
新增一个模块，文案大意为：

- 不知道怎么使用？查看视频教程

要求：
- 提供一个按钮或链接入口
- 当前视频还没有录制，所以点击后不要跳转到真实视频
- 点击后弹出提示：
  - 视频未录制
- 这是占位功能，但样式要完整，不要敷衍

2. 合作联系模块（改为独立页面，不使用弹窗）
请新增一个独立的“合作联系 / 商务合作”页面，而不是使用弹窗 / modal。

目标：
这个页面用于面向老师、教育工作者、学校、培训机构或其他合作方，展示我可以提供的定制服务与合作流程。整体风格应专业、清晰、可信，而不是广告弹窗式的展示。

主页要求：
- 在主页新增一个模块，文案大意为：
  - 您是老师或教育工作者？
  - 有意向与我合作或提出意见？点此联系我。
- 模块中应有一个明确的按钮，例如：
  - 联系我
  - 了解合作
  - 查看合作方案
- 点击按钮后，跳转到一个新的独立页面，例如：
  - `/contact`
  - 或 `/cooperation`

新页面要求：
请创建一个完整的独立页面，整体风格应：
- 专业
- 简洁
- 清晰
- 有可信度
- 更像产品合作说明页，而不是营销广告页

页面结构建议：
1. 顶部标题区
- 标题示例：
  - 合作联系
  - 定制合作
  - 教育合作方案
- 副标题示例：
  - 面向老师、学校、教育工作者与团队的定制化词汇学习解决方案
  - 如果您希望定制专属词库、品牌化页面或专属学习功能，欢迎联系我

2. 我能为您做什么？
请单独作为一个清晰的内容模块，展示以下内容：

【我能为您做什么？】
1. 自定义单词功能，无论是学习、测试、单词游戏等自定义功能
2. 自定义您的单词词库，可以是课本或您自己的词库
3. 不受限制的词库与单词数量设置
4. 将您的团队 / 单位的 logo 标识放在主页或您指定的位置，更加醒目，彰显品牌文化
5. 将网站完全自定义成您想要的样子，或者打包成 app，随您所欲

要求：
- 每一条内容都应排版清晰
- 可以做成卡片列表、分点模块或有层级的说明区
- 不要只是简单堆文本

3. 需要您做什么？
请作为另一个独立模块展示：

【需要您做什么？】
1. 添加我的联系方式
2. 说出您的需求
3. 由我们协商价格后，您需要先支付 30% 的费用
4. 等待项目完成，通常为几周或几月
5. 交付项目，并提供售后服务
6. 您支付余下 70% 的费用

要求：
- 这部分看起来应更像“合作流程”或“合作步骤”
- 可使用步骤条、分步卡片、时间线或简洁的分段结构
- 但不要做得花哨，仍要保持专业感

4. 页面底部增加联系引导区
- 在页面底部增加一个“联系我”引导区域
- 当前不需要接入真实表单，也不需要真实发送消息
- 可以先展示占位信息，例如：
  - 微信联系
  - 邮箱联系
  - 联系方式后续补充
- 或提供一个按钮，点击后提示“联系方式后续补充”

设计要求：
- 不要做成弹窗
- 要像一个真正可以对外展示的合作说明页面
- 视觉风格比普通功能页更稳重、更专业
- 不要像促销页，不要过度营销化
- 与现有网站整体风格保持一致，但允许更偏“展示页 / 商务页”

实现要求：
- 主页上的“联系我”入口跳转到这个独立页面
- 页面可被单独访问
- 响应式布局正常
- 文案层级清晰，阅读体验好

验收标准：
- 主页中存在合作联系入口
- 点击后进入独立页面，而不是弹窗
- 页面包含：
  - 顶部介绍区
  - “我能为您做什么”模块
  - “需要您做什么”模块
  - 底部联系引导区
- 页面整体风格专业、简洁、可信

==================================================
三、词库管理页重构为三栏结构
==================================================

请将当前“默认词库 + 我的词库”的两栏结构，改成三栏结构。

目标顺序必须为：
1. 默认词库
2. 选择课本
3. 我的词库

1. 页面结构调整
- 当前只有两个栏目：
  - 默认词库
  - 我的词库
- 请改成三个栏目，顺序固定为：
  1. 默认词库
  2. 选择课本
  3. 我的词库

2. 新增中间栏目：选择课本
- 在默认词库和我的词库之间新增一个独立栏目，标题为“选择课本”
- 这个栏目未来用于承载课本数据
- 目前系统内还没有导入课本数据，因此当前应显示空状态
- 空状态文案要合理，例如：
  - 暂无课本数据
  - 后续可导入课本内容
- 不要只放一行字，要有完整的空状态区域结构

3. 为后续课本导入预留结构
- 虽然当前没有课本数据，但“选择课本”栏目必须预留后续扩展能力
- 后续如果导入课本，应能像其他栏目一样显示为卡片列表
- 代码结构上应便于未来接入数据源

4. 三个栏目标题前增加 emoji
为了增强识别度，请给三个栏目标题加 emoji。

建议：
- 📚 默认词库
- 📖 选择课本
- 🗂️ 我的词库

如果你有更合适的 emoji，也可以替换，但要语义准确、风格统一。

5. 布局要求
- 桌面端优先三栏展示
- 窄屏下可自然折叠成上下排列
- 三栏卡片宽度、高度、间距、圆角、阴影保持协调一致
- 不影响现有默认词库和我的词库功能

验收标准：
- 页面由两栏变为三栏
- 顺序为：默认词库、选择课本、我的词库
- 三个栏目标题前都有 emoji
- 中间栏目可正常显示空状态
- 后续可扩展为课本数据展示区
- 默认词库与我的词库功能保持正常

==================================================
四、仪表盘重构为“学习控制台”
==================================================

请对仪表盘页面进行一次完整但克制的重构优化，不要只做表面换皮。

目标：
把当前仪表盘从“静态统计页”优化成“学习控制台”，让用户进入页面后能够：
1. 快速看懂当前学习状态
2. 明确下一步该做什么
3. 获得轻量的学习反馈与进度感
4. 保持简洁、现代、学习类产品风格

--------------------
1. 顶部欢迎区 + 主操作区
--------------------

当前“欢迎回来”可以保留，但要优化得更有引导性。

要求：
- 保留欢迎语
- 增加一句更有产品感的副文案，例如：
  - 今天继续完成你的单词练习
  - 从这里继续测试、复习或领读

同时增加一个明显的主操作区。

主操作区建议包含：
- 开始今日测试
- 去错词复习
- 进入词库管理
- 单词领读

要求：
- 至少有一个最突出的主按钮（primary CTA）
- 不要再只是“进入词库并开始测试”这种模糊提示
- 视觉上这个区域要成为仪表盘第一关注点

--------------------
2. 学习概览卡片区
--------------------

请保留顶部概览卡片，但改成更有学习意义的内容。

建议优先展示以下 4 项：
- 可开始学习的词库数量
- 待复习错词数量
- 最近学习记录数量 / 今日学习量
- 最近正确率 / 近期正确率

要求：
- 卡片数量控制在 3~4 个
- 卡片文案更自然，更像学习产品
- 数字层级明确
- 颜色克制，不要刺眼
- 如某项数据当前没有可靠来源，可先做合理占位，但请说明

--------------------
3. 推荐动作 / 下一步建议区
--------------------

新增“推荐动作”模块，增强行为引导。

推荐逻辑示例：
- 如果有错词：优先建议“去复习错词”
- 如果错词为 0：建议“开始新一轮测试”
- 如果某个词库完成度较高但未完成：建议“继续完成 XX 词库”
- 如果新建了词库还没测试：建议“开始第一次测试”

要求：
- 至少展示 1 条推荐动作
- 可先使用简单规则实现，不需要复杂算法
- 文案必须明确，不要空泛

--------------------
4. 最近学习记录区优化
--------------------

保留“最近学习记录”模块，但要重构得更紧凑、更清晰。

要求：
- 默认只显示最近 5 条记录
- 增加“查看全部”或跳转入口
- 每条记录更紧凑，突出重点信息
- 每条记录展示建议：
  - 单词
  - 中文释义
  - 用户答案（如有必要）
  - 所属词库
  - 对错状态
  - 时间
- 状态标签优化：
  - 正确：绿色轻标签
  - 错误：红色轻标签

--------------------
5. 页面节奏与留白优化
--------------------

要求：
- 顶部欢迎区、概览区、推荐区、记录区之间层级清晰
- 不要让所有模块都长得一样
- 控制留白和卡片间距，让页面更有呼吸感
- 不破坏全局 sticky footer 布局

--------------------
6. 仪表盘文案优化
--------------------

请顺手优化仪表盘文案，使它更像学习产品，而不是后台面板。

例如：
- 可学习词库 → 可开始学习的词库
- 错词数量 → 待复习错词
- 下一步 → 继续学习
- 进入词库并开始测试 → 开始今日测试

要求：
- 文案简洁自然
- 避免后台管理式语言
- 保持中文界面风格统一

==================================================
五、实现要求
==================================================

1. 直接修改现有项目代码
- 不要只分析
- 不要只给伪代码
- 输出必须可运行

2. 保持当前技术栈不变
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth

3. 代码要求
- 优先保证可运行
- 优先保证 UI 一致性
- 不要过度抽象
- 尽量复用现有组件
- 必要时可新增少量组件
- 对关键修改点补充简洁注释

4. 样式要求
- 保持简洁、现代、学习类产品风格
- 不要引入复杂图表库
- 不要做花哨大屏风格
- 响应式布局保持正常

==================================================
六、输出要求
==================================================

完成后请输出：
1. 修改了哪些文件
2. 每个文件的核心改动
3. 哪些功能是完整实现
4. 哪些功能目前是 MVP / 占位实现
5. 如何验证这些改动
6. 仪表盘重构了哪些核心区域