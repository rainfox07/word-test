第一、这个页面存在明显的 UI 可读性问题，主要集中在右侧区域和顶部主操作区：

1. 右侧卡片 / 模块文字颜色异常过浅
- 右侧模块中的标题、正文、步骤文字都发白
- 与背景对比度太低，几乎不可读
- 左侧模块文字正常，右侧模块异常，说明不是全局字体问题，而是局部样式问题

2. 顶部主操作区也存在同类问题
- “学习控制台 / 继续学习”等区域的标题、说明文案、按钮文字对比度不足
- 整体看起来像被错误加了 opacity、text-muted、disabled 样式，或者浅色主题下使用了不合适的前景色 token

3. 视觉表现不一致
- 同一页面左侧模块正常，右侧模块和顶部区域异常发白
- 这导致页面信息层级失衡，看起来像局部组件未正确适配当前主题

请修复目标：
- 让右侧模块和顶部主操作区的标题、正文、按钮文字恢复正常可读性
- 提高文字与背景的对比度
- 保持整体简洁、现代风格不变
- 不要简单粗暴改成纯黑，仍要保留原有设计层级，但必须清晰可读

请优先排查：
- 是否使用了错误的 `text-white/xx`、`text-slate-xxx/xx`、`opacity-*`
- 是否误用了 `text-muted`、`disabled`、`foreground token`
- 是否某些卡片容器继承了错误的透明度或浅色主题变量
- 是否右侧和顶部模块使用了与左侧不同的 class 组合，导致文字发灰发白

第二、

请重构首页底部这两个模块的布局和视觉表现，不要再保持现在这种左右两个普通卡片并排的形式。

目标：
把“视频教程”和“合作联系”这两个模块做得更有层次、更图形化、更有视觉吸引力，同时保持整体页面简洁、现代的风格。

具体要求如下：

1. 整体布局调整
- 当前这两个模块是左右并排的普通卡片
- 请改成“上下有层次感的组合布局”，不要只是简单维持现有双卡片并排
- 但在当前这一屏中，仍然保持：
  - 视频教程区域在左侧
  - 联系我区域在右侧
- 两个模块之间要形成更明显的视觉区分和功能导向

2. 左侧模块：视频教程
请保留“视频教程”模块在左侧，但重新设计它的表现方式。

要求：
- 左侧仍然是“不会使用？查看视频教程”这个方向
- 不要只是文字 + 按钮
- 请增加一些图形化说明元素，例如：
  - 流程图标
  - 播放图标
  - 学习步骤小图标
  - 简单的信息图形（例如 导入 → 播放 → 拼写 → 记录）
- 让用户一眼看出这是“新手引导 / 使用教程”区域
- 视觉重点是“帮助快速上手”

3. 右侧模块：合作联系
请把“联系我 / 合作联系”模块放在右侧，并增强它的合作感与商务感。

要求：
- 右侧保留“合作联系”方向
- 请增加一个更能凸显“合作 / 商务 / 定制服务”氛围的视觉元素
- 可以是：
  - 握手图标
  - 合作连接图标
  - 品牌 / 定制服务感的抽象图形
  - 更有商务气质的 icon 区域
- 目标是用户一眼就知道：这不是普通帮助按钮，而是“合作入口”

4. 视觉层级要求
- 左侧模块更像“新手引导 / 教程入口”
- 右侧模块更像“合作 / 商务 / 定制服务入口”
- 两个模块要有明显差异化，不要长得太像
- 但整体风格必须统一，不能一个像学习产品，一个像电商广告

5. 样式要求
- 保持当前网站的简洁、现代、轻学习产品风格
- 不要做复杂插画
- 优先使用：
  - icon
  - 轻量图形组合
  - 简洁信息可视化
- 保持按钮风格统一
- 响应式布局不能坏

6. 结果预期
调整后应达到：
- 左侧：更像“视频教程 / 快速上手”说明区
- 右侧：更像“合作联系 / 商务合作”入口区
- 页面底部这两个模块比现在更有设计感和辨识度