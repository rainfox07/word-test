add feature: 
- 将，位置在“最近学习记录”上面的那个“推荐动作”的入口和卡片全部删除。只保留学习控制台展示的那个推荐动作
- 当用户的鼠标移动到主页上面的bar的克拉斯单词记忆的时候，克拉斯单词记忆这个文本播放一个好看的动画效果


bug fix:

- 在单词测试中，在判定当前单词已经答对正确之前，点击下一题判定为当前答案错误，显示错误的单词。

optimize:

- 在仪表盘，删除词库应该放在该词库卡片的右下角，并且偏小，并且字体为暗红色。
- 选择词库，当鼠标移动到该按钮上时应该有一个图形反馈
- 错题本那里的读音来源的前端排版有点问题，需要优化
- 如果词库数量大于4个，进行一个分页，一页展示4个词库。
- 将快速上手和反馈与建议，在主页的展示界面简化，但分页面的内容保持不变。

请在当前项目中完成以下功能新增、Bug 修复和前端优化。请直接修改代码，不要只给建议。修改前先理解现有页面结构、组件拆分和状态逻辑，尽量保持原有技术栈、设计风格和组件结构，不要大幅重构，不要引入新依赖，除非绝对必要。

一、Feature：功能新增

1. 删除主页中重复的“推荐动作”入口和卡片

当前主页中，在“最近学习记录”上方有一个“推荐动作”入口/卡片区域。请将这个区域完整删除。

注意：
- 只删除“最近学习记录”上方的那一组“推荐动作”入口和卡片。
- 保留“学习控制台”区域中已经存在的“推荐动作”展示。
- 删除后页面布局不能出现明显空白或间距异常。
- 如果该区域对应单独组件，请删除引用即可；不要误删仍被“学习控制台”使用的推荐动作组件或数据。

2. 为主页顶部导航栏的“克拉斯单词记忆”添加鼠标悬停动画

当用户鼠标移动到主页顶部 bar 中的“克拉斯单词记忆”文本时，请添加一个美观、轻量的动画效果。

要求：
- 只在 hover 时触发。
- 动画应自然、精致，不要过于夸张。
- 可以使用 Tailwind CSS 实现，例如轻微放大、渐变文字、下划线滑入、发光阴影、字距变化等。
- 不要引入新的动画库。
- 动画结束后能够平滑恢复原状态。
- 保持移动端和桌面端显示正常。

二、Bug Fix：问题修复

1. 修复单词测试中“下一题”错误判定逻辑

当前问题：
在单词测试页面中，如果当前单词还没有被判定为答对之前，用户点击“下一题”，系统会把当前答案判定为错误，并显示错误的单词。

期望行为：
- “下一题”按钮不应该在未完成当前单词判定时，直接把当前答案判定为错误。
- 如果用户还没有提交/检查当前答案，点击“下一题”时，应优先进行合理处理：
  - 方案 A：禁用“下一题”按钮，直到当前单词已经完成判定；
  - 或方案 B：点击“下一题”时仅跳过当前题，不记录为错误，不显示错误单词；
  - 优先采用方案 A，逻辑更清晰。
- 已经答对、答错或主动提交后的题目，再允许进入下一题。
- 修复后不要影响正常的答题、判题、错词记录和测试进度统计。

三、Optimize：前端与交互优化

1. 优化仪表盘中词库卡片的“删除词库”按钮

当前“删除词库”按钮位置和样式不够合理。请调整为：
- 放在对应词库卡片的右下角。
- 按钮尺寸偏小，视觉权重降低。
- 字体颜色使用暗红色。
- hover 时可以略微加深颜色或出现轻微背景反馈。
- 不要影响词库卡片其他主要操作按钮的布局。

2. 优化“选择词库”按钮的 hover 反馈

当前“选择词库”按钮在鼠标悬停时反馈不明显。请增加图形化或视觉反馈。

要求：
- hover 时应有明显但克制的反馈，例如背景变化、边框变化、阴影、轻微位移、图标变化或右箭头滑入。
- 保持整体设计一致，不要过度花哨。
- 不要引入新依赖。

3. 优化错题本中“读音来源”的前端排版

当前错题本页面里的“读音来源”区域排版存在问题。请检查并优化该区域的布局。

要求：
- 让“读音来源”相关按钮/选项排列更整齐。
- 桌面端和移动端都要适配。
- 文本、按钮和选择控件之间间距合理。
- 不要遮挡、溢出或换行混乱。
- 保持与项目其他设置项的视觉风格一致。

4. 仪表盘词库分页

当词库数量大于 4 个时，对词库列表进行分页。

要求：
- 每页展示 4 个词库。
- 当词库数量不超过 4 个时，不显示分页控件。
- 分页控件需要支持上一页、下一页，以及当前页码展示。
- 删除词库后，如果当前页没有数据，应自动回退到上一页或保持在合法页码。
- 分页逻辑不能影响词库的选择、编辑、删除、统计展示等原有功能。
- 保持响应式布局正常。

5. 简化主页中“快速上手”和“反馈与建议”的展示

请只简化主页上的展示界面，不要修改它们各自独立分页面的完整内容。

要求：
- 主页中的“快速上手”模块只保留简短摘要和进入详情页的入口。
- 主页中的“反馈与建议”模块只保留简短摘要和进入详情页的入口。
- 分页面中的完整说明、卡片、详细内容保持不变。
- 首页展示应更轻量，避免占用过多纵向空间。
- 入口按钮文案可以使用“查看快速上手”“提交反馈”或“查看详情”等。
- 保持页面整体风格统一。

四、完成后的自检要求

请完成修改后进行以下检查：

1. 搜索并确认首页中“最近学习记录”上方的重复“推荐动作”已经删除，但“学习控制台”中的推荐动作仍然存在。
2. 检查顶部导航栏“克拉斯单词记忆”的 hover 动画正常。
3. 检查单词测试流程：
   - 未判题时不能直接点击“下一题”导致记错；
   - 判题后可以正常进入下一题；
   - 错词记录逻辑正常。
4. 检查仪表盘：
   - 删除按钮位于词库卡片右下角；
   - 词库超过 4 个时分页正常；
   - 删除当前页最后一个词库时页码不会异常。
5. 检查错题本读音来源区域在桌面端和移动端的排版。
6. 检查主页“快速上手”和“反馈与建议”已简化，但对应分页面完整内容未被删除。
7. 如果项目有 lint/typecheck/build 命令，请运行对应命令；如果无法运行，请说明原因。

请输出：
- 修改了哪些文件；
- 每个文件的主要改动；
- 是否运行了检查命令；
- 是否还有需要我手动确认的地方。


