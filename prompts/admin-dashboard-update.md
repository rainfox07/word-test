add feature: 

- 一个admin面板，实现以下功能：
    - 有多少用户
    - 用户的正确率（按总的练习量算）
    - 用户错的最多的单词top
    - 

- 词库未完成学习按钮引导不够
- 设置按钮引导不够，告诉用户在哪。
- 邀请码功能：
    - 添加邀请码，邀请码的格式是2510560001 - 2510560300，邀请码使用一次之后作废。只有拥有邀请码才能注册账号。
    - 添加忘记密码功能，如果用户忘记了密码，可以通过邀请码 + 用户昵称重制密码。
    - 将邮箱注册账号更改为邮箱/手机号注册账号，手机号的格式为例如18837000038的数字格式
    - 添加一个简单的人机验证，内容是点击最像苹果/葡萄等水果等颜色，要求具有辨识度。


请在当前项目中实现注册邀请码、忘记密码重置、邮箱/手机号注册、人机验证相关功能。请直接修改代码，不要只给建议。修改前先理解当前项目的认证实现、数据库 schema、Better Auth 配置、注册/登录页面、服务端 action/API 结构和现有表结构。尽量保持现有技术栈、设计风格和组件结构，不要大幅重构，不要引入新依赖，除非绝对必要。

一、Add Feature：邀请码注册功能

1. 新增邀请码机制

邀请码格式范围：
2510560001 - 2510560300

要求：
- 系统内置 300 个邀请码。
- 每个邀请码只能使用一次。
- 用户注册账号时必须填写邀请码。
- 没有邀请码不能注册。
- 邀请码不存在、格式错误、已被使用，都要阻止注册并给出明确提示。
- 邀请码使用成功后立即标记为已使用，不能再次注册。
- 邀请码需要记录：
  - code：邀请码字符串
  - used：是否已使用
  - usedByUserId：被哪个用户使用
  - usedAt：使用时间
  - createdAt：创建时间
- 注册用户成功和邀请码标记已使用应尽量保持原子性，避免用户创建成功但邀请码没有失效，或邀请码失效但用户没有创建成功。

2. 数据库设计

请根据当前项目的 Drizzle schema 新增 invitationCodes 表，或使用更合适的命名。

建议字段：
- id
- code，唯一，非空
- used，boolean，默认 false
- usedByUserId，可为空，关联用户 id
- usedAt，可为空
- createdAt

如果当前数据库是 SQLite/libSQL，请使用适配当前数据库的字段类型。

3. 初始化邀请码数据

请添加或修改 seed 脚本，生成并插入 2510560001 到 2510560300 这 300 个邀请码。

要求：
- seed 必须是幂等的。
- 多次运行 seed 不应重复插入邀请码。
- 已经被使用的邀请码不能被重置为未使用。
- 如果邀请码已存在，跳过或保持原状态。

4. 注册页面改造

注册页面需要新增邀请码输入框。

注册时校验：
- 邀请码不能为空。
- 邀请码必须是 10 位数字字符串。
- 邀请码必须在 2510560001 - 2510560300 范围内。
- 邀请码必须存在且未使用。
- 注册成功后邀请码作废。

请注意前端校验只能作为体验优化，最终校验必须在服务端完成。

二、Add Feature：忘记密码功能

1. 新增忘记密码入口

在登录页面添加“忘记密码”入口，跳转到忘记密码页面，或者在同页面以表单形式展示。

2. 忘记密码重置方式

用户忘记密码后，可以通过以下信息重置密码：
- 邀请码
- 用户昵称
- 新密码
- 确认新密码

逻辑要求：
- 用户输入的邀请码必须是该用户注册时使用过的邀请码。
- 用户昵称必须与对应账号匹配。
- 邀请码 + 用户昵称匹配成功后，允许重置密码。
- 新密码和确认新密码必须一致。
- 新密码需要符合当前项目已有密码强度要求。如果项目没有要求，至少要求 8 位以上。
- 重置成功后，应更新用户密码为新的安全哈希值，不能明文存储。
- 不要把旧密码返回到前端。
- 不要提供“查询密码”功能，只允许重置密码。
- 错误提示要适度，不要暴露过多账号枚举信息。例如可以提示“邀请码或用户昵称不匹配”。

3. 安全要求

- 密码必须使用当前认证系统的哈希方式保存，不能手写明文存储。
- 如果项目使用 Better Auth，请优先调用或复用 Better Auth 推荐的密码更新/哈希逻辑。
- 如果当前项目已有封装的更新密码方法，请复用现有逻辑。
- 忘记密码接口必须运行在服务端。
- 不要在客户端直接访问数据库。

三、Add Feature：邮箱/手机号注册账号

当前注册方式是邮箱注册。请改造为“邮箱或手机号均可注册”。

1. 注册账号输入项

将注册表单中的邮箱字段改为“邮箱/手机号”。

用户可以输入：
- 邮箱，例如 user@example.com
- 手机号，例如 18837000038

2. 手机号格式要求

手机号格式：
- 必须是 11 位数字
- 示例：18837000038
- 暂不要求短信验证码
- 暂不接入真实短信服务

3. 邮箱格式要求

如果输入内容包含 @，按邮箱处理，并使用当前项目已有邮箱校验逻辑。

如果输入内容不包含 @，按手机号处理，并校验 11 位数字。

4. 数据库与认证兼容

请检查当前认证系统是否强依赖 email 字段。

如果 Better Auth 或现有 user 表必须使用 email：
- 可以新增 phone 字段保存手机号；
- 对手机号注册用户，仍需保证认证系统可正常创建和登录；
- 可以根据项目现有认证限制，使用内部占位 email，例如 phone_18837000038@local.user，但前端和用户侧展示为手机号；
- 必须避免不同手机号产生冲突；
- 必须保证邮箱和手机号都不能重复注册。

如果项目可以直接支持 phone 字段登录，请优先使用 phone 字段。

5. 登录页面同步改造

登录页面也应支持“邮箱/手机号 + 密码”登录。

要求：
- 用户输入邮箱时按邮箱登录。
- 用户输入手机号时按手机号登录。
- 如果手机号注册实际使用了内部占位 email，需要在服务端登录逻辑中自动映射到对应账号。
- 错误提示保持友好，不暴露过多账号信息。

四、Add Feature：简单人机验证

在注册页面添加一个简单的人机验证。

验证形式：
用户需要根据提示点击最符合要求的图形/颜色选项，例如：
- 点击最像苹果的图案
- 点击最像葡萄的图案
- 点击红色水果
- 点击紫色水果

要求：
1. 不引入复杂验证码服务。
2. 不接入第三方验证码。
3. 使用项目内置的简单图形或 emoji 即可，但必须有辨识度。
4. 每次注册页面加载时，可以随机生成一个题目。
5. 用户必须选择正确选项后才能提交注册。
6. 前端需要有交互反馈：
   - 未选择时提示先完成人机验证；
   - 选择错误时提示重新选择；
   - 选择正确时显示通过状态。
7. 服务端也要进行基本校验，不能只依赖前端状态。
8. 不要把正确答案写成明显暴露在 DOM 中的字段。如果实现成本较高，至少不要仅通过一个可随意修改的前端 boolean 来决定是否通过。
9. 保持 UI 简洁，适合当前注册页面风格。

建议题库示例：
- 题目：请选择苹果；选项：🍎 🍇 🍌 🍉
- 题目：请选择葡萄；选项：🍎 🍇 🍌 🍉
- 题目：请选择黄色水果；选项：🍎 🍇 🍌 🍉；正确答案：🍌
- 题目：请选择红色水果；选项：🍎 🍇 🍌 🍉；正确答案：🍎
- 题目：请选择西瓜；选项：🍎 🍇 🍌 🍉

五、页面和体验要求

1. 注册页面需要包含：
- 昵称
- 邮箱/手机号
- 密码
- 确认密码，如当前已有则保留；如没有请新增
- 邀请码
- 简单人机验证
- 注册按钮

2. 登录页面需要包含：
- 邮箱/手机号
- 密码
- 忘记密码入口

3. 忘记密码页面需要包含：
- 用户昵称
- 注册时使用的邀请码
- 新密码
- 确认新密码
- 重置密码按钮

4. 所有错误提示要清楚：
- 邀请码不存在或已使用
- 邀请码格式错误
- 邮箱或手机号格式错误
- 邮箱或手机号已被注册
- 密码不一致
- 人机验证未通过
- 邀请码或用户昵称不匹配

5. 保持页面整体视觉风格统一，移动端也要正常显示。

六、兼容性和迁移要求

1. 不要破坏已有用户登录。
2. 已经用邮箱注册的老用户仍然可以用邮箱登录。
3. 如果新增 phone 字段，需要确保旧用户 phone 为空时不会报错。
4. 如果 user 表已有 name/nickname 字段，请复用；如果没有，请根据当前项目结构新增 nickname 字段。
5. 如果新增数据库字段或表，请同步修改：
- Drizzle schema
- 迁移/生成逻辑
- seed 脚本
- 相关类型
- 服务端注册/登录/重置密码逻辑
- 前端表单类型

七、完成后的自检要求

请完成后进行以下检查：

1. 数据库检查：
- invitationCodes 表是否创建成功；
- 2510560001 - 2510560300 是否能被 seed 幂等插入；
- 多次运行 seed 不会重复插入；
- 已使用邀请码不会被 seed 重置。

2. 注册检查：
- 不填邀请码不能注册；
- 填错误邀请码不能注册；
- 填已使用邀请码不能注册；
- 填有效邀请码可以注册；
- 注册成功后邀请码立刻变为已使用；
- 邮箱注册可用；
- 手机号注册可用；
- 重复邮箱不能注册；
- 重复手机号不能注册；
- 人机验证错误不能注册；
- 人机验证正确才能注册。

3. 登录检查：
- 老邮箱用户仍可登录；
- 新邮箱用户可登录；
- 新手机号用户可登录；
- 错误密码不能登录。

4. 忘记密码检查：
- 邀请码 + 用户昵称匹配时可以重置密码；
- 邀请码 + 用户昵称不匹配时不能重置；
- 新密码和确认密码不一致时不能重置；
- 重置后旧密码不能登录，新密码可以登录；
- 密码没有明文存储。

5. 如果项目有 lint/typecheck/build 命令，请运行对应命令；如果无法运行，请说明原因。

八、输出要求

修改完成后请输出：
- 修改了哪些文件；
- 每个文件的主要改动；
- 新增了哪些数据库表或字段；
- seed 如何运行；
- 是否需要我执行 pnpm db:generate、pnpm db:push、pnpm db:seed；
- 是否运行了 lint/typecheck/build；
- 是否还有需要我手动确认的地方。




你正在维护一个名为 `word-test` 的全栈项目。请直接在现有代码基础上完成以下功能增强与引导优化，输出可运行结果，不要只给建议，也不要只写伪代码。

目标：
新增一个可用的 admin 面板，用于查看用户学习情况；同时增强词库未完成学习与设置入口的用户引导，让用户更容易理解下一步该做什么。

整体要求：
- 保持当前项目简洁、现代、学习类产品风格
- 保持中文界面统一
- 优先保证可运行、可验证、可维护
- 不要过度抽象
- 响应式布局不能坏

技术栈保持不变：
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth

==================================================
一、新增 Admin 面板
==================================================

请新增一个 admin 面板页面，用于查看整站用户学习情况。

目标：
这个页面不是普通用户可见页面，而是管理员使用的后台概览页，用来查看用户数量、用户学习表现、常见错误词等信息。

1. 路由与权限
- 新增一个 admin 页面，例如：
  - `/admin`
- 进入该页面时需要输入密码
- 密码为:DzQBO3kWvpPWgVf0yr9nshigc69fhK0wsNJYTImuEbM=

2. 管理员身份识别
请基于当前项目现状，使用最简单可维护的方式识别管理员。


3. Admin 面板展示内容
请至少展示以下模块：

3.1 用户总数
- 展示当前系统中一共有多少用户
- 数字要明显，适合作为统计卡片

3.2 用户学习表现列表
为每个用户展示一个学习概览，至少包括：
- 用户昵称 / 用户名
- 用户邮箱（如果适合展示）
- 总练习量
- 总正确率

正确率计算方式：
- 正确率 = 该用户所有 test_records 中正确数量 / 总答题数量
- 按总练习量计算，不是最近一次
- 如果用户还没有练习记录，正确率要合理处理（例如显示 0% 或“暂无记录”）

展示要求：
- 列表或表格都可以
- 需要便于阅读
- 正确率建议显示为百分比
- 总练习量建议显示具体数字

3.3 用户错得最多的单词 Top
请增加一个“错误词排行榜”模块，用于展示全站或用户维度下最常错的单词。

最少先实现一个全站 Top 模块，展示例如：
- 单词
- 中文释义
- 错误次数

如果实现成本合适，也可进一步扩展为：
- 每个用户的错误 Top 单词
- 或点击某个用户后查看该用户的错误词 Top

要求：
- 至少显示 Top 5 或 Top 10
- 统计逻辑清晰
- 基于 test_records 中错误记录计算
- 不要重复算同一条记录

4. 建议你顺手补充的 admin 信息
- 今日活跃用户数
- 今日练习量
- 最近新增用户数
- 最近新建词库数
- 完成度最高的词库 / 使用最多的词库

这些是加分项，不是硬性必须。

5. Admin 面板 UI 要求
- 风格可以比普通页面更偏“后台概览”，但仍要保持与整体项目一致
- 推荐结构：
  - 顶部统计卡片
  - 中部用户列表 / 表格
  - 底部排行榜 / 错误分析模块
- 不要做复杂图表库依赖
- 优先使用卡片、表格、列表等简单但清晰的方式

==================================================
二、增强“未完成学习”按钮引导
==================================================

当前词库页或仪表盘中，对于“继续未完成学习 / 未完成学习”相关按钮的引导还不够明确，用户不容易理解为什么要点它、点了会发生什么。

请优化这部分的引导表达。

要求：
1. 文案更明确
- 不要只写“继续学习”或类似模糊词
- 要让用户看懂：
  - 这是继续某个未完成的词库
  - 或这是继续上次未完成的测试 / 学习流程

可参考方向：
- 继续完成该词库
- 继续上次未完成的测试
- 该词库还未学完，点此继续
- 还差 X% 完成，继续进入

2. 补充上下文说明
- 在按钮附近增加简短说明文字
- 让用户知道为什么推荐这个动作
- 比如：
  - “这个词库已经完成 80%，再进行一轮测试就能收尾”
  - “你上次中断在这里，继续可保持学习节奏”

3. 强化视觉引导
- 按钮要足够显眼，但不要过度夸张
- 可以通过按钮层级、说明文字、状态标签来突出
- 保持整体风格统一

==================================================
三、增强“设置按钮”引导
==================================================

当前设置按钮的存在感和引导性不足，用户不容易意识到设置入口在哪里，也不知道设置功能能做什么。

请优化设置按钮的引导，让用户能更容易找到设置入口并理解其用途。

要求：
1. 告诉用户设置在哪
- 请在合适的页面中增加轻量提示，引导用户注意设置按钮
- 不要太吵，但要清楚
- 例如可用：
  - 小提示文案
  - 轻提示条
  - 首次引导文本
  - icon + label 的方式

2. 告诉用户设置能做什么
根据你当前项目已有设置能力，向用户说明设置入口可调整哪些内容，例如：
- 播放相关设置
- 学习模式设置
- 领读设置
- 偏好设置

3. 视觉层级优化
- 如果当前设置按钮过于隐蔽，请适度增强其存在感
- 可以优化图标、位置、文案或与页面结构的关系
- 但不要把它做成打断体验的强提醒

4. 首次引导优先
- 如果适合，可以做成首次进入页面时显示一次的轻提示
- 或者用 localStorage 控制只提示一次
- 不要求复杂引导系统，MVP 可用即可

==================================================
四、实现要求
==================================================

1. 直接修改现有项目代码
- 不要只分析
- 不要只给伪代码
- 输出必须可运行

2. 代码要求
- 优先保证功能能实际用
- 优先保证 UI 一致性
- 不要过度抽象
- 尽量复用现有组件
- 必要时可新增少量文件
- 对关键逻辑加简洁注释

3. 数据要求
- Admin 页面中的统计要基于真实数据库数据
- 正确率和错误词 Top 必须基于 test_records 计算
- 管理员权限判断要真实生效，不只是前端隐藏

==================================================
五、验收标准
==================================================

完成后请确保以下内容可验证：

1. 新增 `/admin` 或等价管理员页面
2. 普通用户无法访问 admin 面板
3. admin 面板中可看到：
- 用户总数
- 每个用户的总练习量
- 每个用户的总正确率
- 错误词 Top 排行
4. “未完成学习”按钮比之前更容易理解
5. 按钮附近有明确说明或更好的引导文案
6. 用户更容易发现设置按钮，并知道设置入口的作用
7. 不破坏现有功能与页面风格

==================================================
六、输出要求
==================================================

完成后请输出：
1. 修改了哪些文件
2. 每个文件的核心改动
3. admin 面板实现了哪些功能
4. 管理员权限是如何判断的
5. 哪些引导文案和交互被优化了
6. 如何验证这些改动



你正在维护一个名为 `word-test` 的全栈项目。请直接在现有代码基础上新增一个“Admin 面板”，用于管理员查看整站用户、学习数据、词库表现和系统状态。不要只给建议，要输出可运行结果。

目标：
做一个轻量但实用的 admin 面板，不走重型企业后台风格，而是保持当前项目简洁、现代、清晰的视觉风格。这个面板的核心作用是帮助管理员快速回答以下问题：

1. 现在有多少人在使用网站
2. 用户整体学得怎么样
3. 大家最容易错在哪里
4. 哪些词库表现好，哪些词库表现差
5. 网站当前是否健康、是否有明显异常

整体要求：
- 优先使用现有技术栈实现
- 不要过度抽象
- 不要引入复杂图表库
- 优先用统计卡片、表格、列表、排行榜、状态模块等方式实现
- 页面应可运行、可验证、可扩展

技术栈保持不变：
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth

==================================================
一、路由与权限控制
==================================================

请新增一个管理员页面，例如：
- `/admin`

- 进入该页面时需要输入密码
- 密码为:DzQBO3kWvpPWgVf0yr9nshigc69fhK0wsNJYTImuEbM=

==================================================
二、页面结构
==================================================

请将 admin 面板设计为以下结构：

1. 顶部总览卡片区
2. 用户学习表现区
3. 错误分析区
4. 词库表现区
5. 最近活动流
6. 系统健康状态区

保持模块层级清晰、布局整洁，避免页面过于拥挤。

==================================================
三、顶部总览卡片区
==================================================

请在 admin 页面顶部增加一组统计卡片，建议至少包含以下内容：

1. 用户总数
- 当前系统中总共有多少注册用户

2. 今日活跃用户数
- 今日至少产生过一次学习行为 / 测试记录的用户数量

3. 总练习次数
- 所有用户累计完成的答题记录总数

4. 今日练习次数
- 今日新增的 test_records 数量

5. 全站平均正确率
- 所有 test_records 中正确数量 / 总记录数量

6. 错词总数
- 全站错误记录总数

要求：
- 数字突出
- 文案简洁
- 卡片风格统一
- 如果某项数据当前不易精准实现，可用合理近似，但要说明

==================================================
四、用户学习表现区
==================================================

请实现一个用户学习表现列表或表格，用来展示每个用户的学习情况。

每个用户建议至少展示以下字段：
- 用户名 / 昵称
- 邮箱
- 注册时间
- 总练习量
- 总正确率
- 错词数量
- 最近活跃时间
- 已创建词库数

正确率计算方式：
- 正确率 = 用户所有 test_records 中正确数量 / 总答题数量
- 按总练习量计算，不是最近一次
- 如果用户没有练习记录，应合理显示为 0% 或“暂无记录”

要求：
1. 列表或表格均可，但必须清晰可读
2. 支持至少一种排序方式，推荐：
   - 按总练习量排序
   - 按正确率排序
3. 尽量让管理员一眼看出：
   - 高活跃用户
   - 学习表现较弱的用户
   - 注册后几乎没使用的用户

==================================================
五、错误分析区
==================================================

请新增一个“错误分析”模块，用来查看用户最容易出错的内容。

至少实现以下模块：

1. 全站错误词 Top 10
展示字段建议：
- 单词
- 中文释义
- 错误次数
- 正确次数
- 错误率
- 所属词库（如可得）

2. 最近 7 天错误词 Top 10
- 按最近 7 天内的错误记录统计
- 用来观察近期问题词汇

3. 各词库中最容易错的单词 Top
- 至少按词库维度显示一个“错误最多单词”摘要
- 如果实现成本不高，可做成每个词库各自的 Top 3 / Top 5

要求：
- 统计逻辑基于真实 test_records 数据
- 排名规则清晰
- 展示应足够直观，不要只输出原始数据

可选增强（推荐但不是必须）：
- 支持点击某个用户查看“该用户错误词 Top”
- 或支持点击某个词库查看该词库详细错误词排行

==================================================
六、词库表现区
==================================================

请新增一个“词库表现”模块，用于观察不同词库的使用与学习效果。

每个词库建议展示以下内容：
- 词库名称
- 词库类型（默认词库 / 用户词库）
- 使用人数
- 总测试次数
- 平均正确率
- 平均完成度
- 错误最多的单词
- 最近是否有人使用

要求：
1. 能帮助管理员判断：
   - 哪些词库更受欢迎
   - 哪些词库几乎没人使用
   - 哪些词库过难或体验有问题
2. 展示方式可为：
   - 表格
   - 排行榜
   - 卡片列表
3. 如果“平均完成度”已有现成计算逻辑，应尽量复用

==================================================
七、用户行为漏斗 / 引导分析（推荐）
==================================================

如果实现成本适中，请新增一个轻量“用户行为漏斗”模块，帮助管理员观察用户在哪一步流失。

建议至少展示以下数据：
- 注册用户数
- 创建过词库的用户数
- 开始过测试的用户数
- 完成至少一次完整测试的用户数
- 使用过错题本的用户数
- 使用过单词领读的用户数

目标：
帮助管理员理解：
- 用户是否注册后就流失
- 用户是否创建了词库但没有真正开始测试
- 用户是否测试了但没有复习错题
- 用户是否使用了领读功能

如果暂时无法精确实现完整漏斗，可先实现简化版本，但请说明。

==================================================
八、最近活动流
==================================================

请新增“最近活动流”模块，用于观察站点近期发生的主要事件。

可展示的事件类型包括但不限于：
- 新用户注册
- 用户创建词库
- 用户导入单词
- 用户完成一轮测试
- 用户清空错题本
- 用户进入单词领读

要求：
- 按时间倒序显示
- 每条活动应包含：
  - 用户
  - 行为
  - 时间
- 尽量让管理员快速感知站点近期动态

如果现有数据结构不足以支持完整活动流，可基于已有表做 MVP 实现，例如从用户表、词库表、test_records 中提取近期活动近似展示。

==================================================
九、系统健康状态区
==================================================

请新增一个轻量“系统状态”模块，用于展示站点当前健康情况。

建议至少包含以下内容：
- 数据库状态：正常 / 异常
- 音频接口状态：正常 / 异常
- 最近 24 小时错误请求数（如不易获取可暂做占位）
- 证书状态 / 域名状态（如可得）
- 当前版本 / 最近部署时间（如可得）

要求：
- 这是一个轻量状态面板
- 不要求复杂监控系统
- 用简单标签或状态卡片展示即可
- 重点是让管理员一眼知道网站是否“健康”

==================================================
十、可操作能力（推荐但不是必须）
==================================================

如果实现成本适中，可以增加少量管理操作能力，例如：
- 查看某个用户的详细学习记录
- 查看某个用户的错词本
- 查看某个词库的详细统计
- 编辑默认词库
- 新增默认词库
- 删除无用默认词库
- 导出错误词排行
- 导出用户学习表现数据

这些不是硬性必须，但如果你认为当前结构容易接入，可以作为加分项实现。

==================================================
十一、UI 与交互要求
==================================================

1. 风格要求
- 保持当前项目简洁、现代、清晰的风格
- 不要做成过重的企业 ERP 样式
- 不要引入复杂图表库
- 优先使用：
  - 统计卡片
  - 表格
  - 排行榜
  - 列表
  - 轻量状态模块

2. 层级要求
- 顶部总览卡片要一眼看懂站点状态
- 中间应重点突出“用户表现”和“错误分析”
- 底部可放活动流和系统状态

3. 响应式要求
- 桌面端信息布局清晰
- 窄屏下自然折叠
- 不要破坏现有导航和整体布局

==================================================
十二、实现要求
==================================================

1. 直接修改现有项目代码
- 不要只分析
- 不要只给伪代码
- 输出必须可运行

2. 数据要求
- 统计应尽量基于真实数据库数据
- 正确率和错误 Top 必须基于 test_records 计算
- 管理员权限必须真实生效，不只是前端隐藏

3. 代码要求
- 优先保证可运行
- 优先保证可读性和可维护性
- 不要过度抽象
- 尽量复用现有组件
- 必要时可新增少量组件和辅助函数
- 对关键统计逻辑加简洁注释

==================================================
十三、验收标准
==================================================

完成后请确保以下内容可验证：

1. 新增 `/admin` 或等价管理员页面
2. 普通用户无法访问 admin 面板
3. admin 面板中可看到：
- 用户总数
- 今日活跃用户数
- 总练习量 / 今日练习量
- 全站平均正确率
- 错误词 Top 排行
- 用户学习表现列表
- 词库表现模块
- 最近活动流
- 系统状态模块
4. 页面风格与项目整体一致
5. 模块层级清晰，信息易于浏览
6. 不破坏现有用户功能与页面结构

==================================================
十四、输出要求
==================================================

完成后请输出：
1. 修改了哪些文件
2. 每个文件的核心改动
3. admin 面板实现了哪些统计项
4. 管理员权限是如何判断的
5. 哪些数据是直接接入真实数据的
6. 哪些模块目前是 MVP / 近似实现
7. 如何验证这些改动