<!-- ## big fix

1. 页面的 footer 布局有问题：

- 当页面内容较少时，footer 没有停留在页面底部
- footer 提前出现在主内容区下方，视觉上显得“太靠前”
- footer 下方还留有大块空白，影响页面完整性

预期效果：
- 当页面内容不足一屏时，footer 应固定在视口底部（sticky footer 效果，不是悬浮 fixed）
- 当页面内容超过一屏时，footer 应自然出现在页面内容末尾
- 不要使用 `position: fixed` 直接钉死在底部
- 应通过页面整体布局实现，比如让外层容器至少占满 `min-h-screen`，并用 flex 布局把 footer 推到底部

请优先检查：
- 根布局是否使用了 `min-h-screen`
- 页面最外层是否是 `flex flex-col`
- 主内容区域是否设置了 `flex-1`
- footer 是否被包在错误的容器里，导致不能自然贴底

2. 测试模式选择中，增加一个返回词库的按钮

3. 将footer的Word Test MVP by Next.js 15 + Drizzle + Better Auth文本改为：
© 2026 Lihyu "\n"
 Based on Next.js 15 + Drizzle + Beteer Auth "\n"
 Github URL : https://github.com/rainfox07/word-test "\n"
 Contact me at Wechat : _3kuxD
 四行文本，居中显示

 4. 请调整会员页面的两张方案卡片布局与内容，重点突出 Plus 至尊版。

    具体修改要求：

    1. 请调整会员页面的两张方案卡片布局与内容，重点突出 Plus 至尊版。


    2. 强调 Plus 至尊版标题区域
    - 当前“普拉斯用户 / PLUS 至尊版”的标题层级还不够突出
    - 请增强这个区域的视觉权重，让用户一眼看到这是主推方案
    - 可以通过更大的标题字号、更明显的颜色对比、更强的字重、角标/标签、轻微高亮背景等方式实现
    - 但整体风格仍要保持简洁现代，不要做得太俗气

    3. 将右侧权益列表放入一个统一的文本框 / 卡片区域内
    - 当前右侧多个权益项看起来是分散的
    - 请把这些内容放进一个统一的容器中，使它更像一个完整的权益说明区域
    - 保持排版整齐，层级清晰，便于阅读

    4. 在 Plus 至尊版卡片下方新增一个 “Subscribe” 按钮
    - 按钮文案就叫：Subscribe
    - 视觉上应与当前页面风格统一，但要足够明显
    - 按钮点击后不要接支付接口
    - 当前阶段点击后只弹出一个提示框 / toast / modal，提示内容类似：
    “支付接口暂未接入，当前仅为展示占位，暂不会收款。”
    - 这个按钮目前只是占位，不做真实支付逻辑

    5. 保持整体页面风格一致
    - 不要破坏现有简洁、现代的学习产品风格
    - 响应式布局不能坏
    - 两张卡片的间距、阴影、圆角、留白保持协调

    验收标准：
    - 左侧是 Plus 至尊版，右侧是 Free
    - Plus 方案比 Free 更显眼
    - Plus 权益内容在统一容器内展示
    - 页面中有 Subscribe 按钮
    - 点击 Subscribe 只弹出“支付接口未接入”的提示，不做实际支付

5. 当在head bar(仪表盘，词库管理)等页面切换时，做一个平滑的切换动画，但时间不超过1秒
6. 在仪表盘前面新加一个主页按钮，与点按head bar的word test的作用相同，回到主页。 -->

你正在维护一个名为 `word-test` 的全栈项目。请直接在现有代码基础上完成以下界面修复与体验优化，输出可运行结果，不要只给建议。

目标：
修复全局布局问题，优化导航体验，完善测试模式页与会员页的交互细节，并保持现有项目的简洁、现代、学习类产品风格。

====================
一、Big Fix
====================

1. 修复 footer 布局问题（Sticky Footer）
当前页面的 footer 布局有问题：

- 当页面内容较少时，footer 没有停留在页面底部
- footer 提前出现在主内容区下方，视觉上显得“太靠前”
- footer 下方还留有大块空白，影响页面完整性

预期效果：
- 当页面内容不足一屏时，footer 应贴在视口底部
- 当页面内容超过一屏时，footer 应自然出现在页面末尾
- 不要使用 `position: fixed`
- 应通过整体布局实现 sticky footer 效果

请优先检查并修复：
- 根布局是否使用了 `min-h-screen`
- 页面最外层是否是 `flex flex-col`
- 主内容区域是否设置了 `flex-1`
- footer 是否被包在错误的容器里，导致不能自然贴底

实现要求：
- 这是全局布局修复，不要只修某一个页面
- 修复后所有主要页面都应受益
- 保持现有样式风格一致

2. 在测试模式选择页增加“返回词库”按钮
在测试模式选择页面中，新增一个按钮，让用户可以返回词库页，而不是只能继续进入测试模式。

要求：
- 按钮文案明确，例如“返回词库”
- 位置合理，不要破坏当前页面布局
- 点击后返回词库管理 / 词库选择页面
- 风格与现有按钮体系保持一致

3. 修改 footer 文案内容与排版
将 footer 中原本的：
`Word Test MVP by Next.js 15 + Drizzle + Better Auth`
替换为以下四行文本，并居中显示：

第 1 行：
© 2026 Lihyu

第 2 行：
Based on Next.js 15 + Drizzle + Better Auth

第 3 行：
Github URL : https://github.com/rainfox07/word-test

第 4 行：
Contact me at Wechat : _3kuxD

要求：
- 四行文本垂直排列
- 整体居中显示
- 保持可读性与简洁风格
- 如果有拼写错误请顺手修正（例如 Better Auth 的拼写）

4. 会员页面改版，突出 Plus 至尊版
请调整会员页面的两张方案卡片布局与内容，重点突出 Plus 至尊版。

具体要求：

4.1 布局调整
- 左侧显示 Plus 至尊版
- 右侧显示 Free
- Plus 方案应作为主推荐方案

4.2 强化 Plus 顶部标题区域
- 当前“普拉斯用户 / PLUS 至尊版”的标题层级不够突出
- 请增强这个区域的视觉权重，让用户一眼看到这是主推方案
- 可以通过更大的字号、更强的字重、更清晰的层级、更明显的高亮方式来实现
- 保持整体风格简洁，不要过度浮夸

额外补充：
- 交换“普拉斯用户”和“PLUS 至尊版”这两行文字的上下位置
- 改为上面显示：PLUS 至尊版
- 下面显示：普拉斯用户
- 并将“普拉斯用户”的字号明显调大，接近当前“PLUS 至尊版”的字号级别
- 注意：这里调整的是 Plus 卡片内部标题区域，不是再交换整张卡片左右位置

4.3 权益列表统一到一个容器中
- 当前 Plus 卡片中的权益项看起来较分散
- 请将这些权益内容放进一个统一的文本框 / 卡片容器中
- 让它更像完整的权益说明区域
- 保持排版整齐、层级清晰、便于阅读

4.4 新增 Subscribe 按钮
- 在 Plus 至尊版卡片下方新增一个按钮
- 按钮文案：`Subscribe`
- 按钮视觉上要明显，但要与现有 UI 风格统一
- 点击后不要接支付接口
- 当前阶段只弹出一个提示框 / toast / modal，提示内容类似：
  `支付接口暂未接入，当前仅为展示占位，暂不会收款。`
- 这是占位逻辑，不做真实支付

4.5 验收标准
- 左侧是 Plus 至尊版，右侧是 Free
- Plus 比 Free 更显眼
- Plus 顶部标题层级更强
- “PLUS 至尊版”在上，“普拉斯用户”在下，且“普拉斯用户”字号明显变大
- Plus 权益内容在统一容器中展示
- 页面中有 Subscribe 按钮
- 点击 Subscribe 只弹出“支付接口未接入”的提示，不做真实支付

5. 导航切换时增加平滑过渡动画
当用户在 head bar 中切换页面（例如：主页、仪表盘、词库管理、会员、错词本）时，增加一个平滑的页面切换动画。

要求：
- 动画自然、简洁，不要太夸张
- 总时长不超过 1 秒
- 不要影响页面可用性和加载性能
- 保持风格克制，符合学习类产品气质

6. 在导航栏中新增“主页”按钮
当前 head bar 中已有“Word Test”品牌入口和多个导航项。
请在“仪表盘”前面新增一个“主页”按钮。

要求：
- 放在导航栏中，位置在“仪表盘”前
- 功能与点击左上角 `Word Test` 品牌一致
- 点击后返回首页
- 保持导航栏整体排版协调

====================
二、实现要求
====================

1. 直接修改现有项目代码
- 不要只分析
- 不要只给伪代码
- 输出应可运行

2. 保持当前技术栈不变
- Next.js 15
- TypeScript
- Tailwind CSS
- App Router
- Drizzle ORM
- Better Auth

3. 代码要求
- 优先保证可运行
- 优先保证样式统一
- 不要过度抽象
- 尽量复用现有布局与组件
- 必要时可新增少量文件
- 对关键修改点补充简洁注释

4. UI 要求
- 保持简洁、现代、学习类产品风格
- 响应式布局不能被破坏
- 间距、圆角、阴影、文本层级保持协调

====================
三、输出要求
====================

完成后请输出：

1. 修改了哪些文件
2. 每个文件的核心改动
3. 修复了哪些问题
4. 新增了哪些交互
5. 如何验证这些改动
6. 哪些功能只是占位实现（例如 Subscribe）